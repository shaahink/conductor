# K1.1 — KataFlow CI and Dependabot disabled on the remote

Repo: `shaahink/KataFlow` (local clone `C:/Code/KataFlow-ai`), default branch `main`.
Captured 2026-08-03 against the real remote with the `gh` CLI.

## Acceptance

1. The `CI` workflow reports `disabled_manually` on the remote.
2. No workflow other than Dependabot's synthetic `Dependabot Updates` entry reports itself active.
3. `.github/dependabot.yml` no longer exists on `main`.

## What was done

- `gh workflow disable ci.yml` — a remote state change, no commit needed. Exit 0.
- `.github/dependabot.yml` removed and pushed to `main` as commit **`f47e28d`**
  (`chore: retire KataFlow - disable Dependabot version updates`),
  `4330a7c..f47e28d  main -> main`.

The Dependabot config was deleted rather than left in place because it is the config
that raises new version-update pull requests; leaving it would let Dependabot re-raise
the PRs that K1.2 closes. The synthetic `Dependabot Updates` workflow entry cannot be
removed and is explicitly carved out of the acceptance.

## Evidence

`gh workflow list --all` after the change:

```
CI                  disabled_manually   290775701
Dependabot Updates  active              290775734
```

`gh api repos/shaahink/KataFlow/contents/.github/dependabot.yml`:

```
{"message":"Not Found","documentation_url":"https://docs.github.com/rest/repos/contents#get-repository-content","status":"404"}
```

`gh run list --limit 5` after the push to `main` — the push produced **no new run**, which
is what a disabled workflow should do. The newest run on `main` is still the pre-existing
red one from 2026-08-02:

```
30765473647  CI                                 main  completed  failure  2026-08-02T20:22:37Z
28334598262  CI    dependabot/nuget/.../Spectre.Console-0.57.1  completed  failure  2026-06-28T20:13:43Z
28334537000  CI    dependabot/nuget/.../Microsoft.NET.Test.Sdk-18.7.0  completed  failure  2026-06-28T20:11:18Z
28334313498  nuget in /. - Update #1437613018   main  completed  success  2026-06-28T20:02:46Z
27915882794  nuget in /. - Update #1426807036   main  completed  success  2026-06-21T20:02:35Z
```

Note for the stage gate: run `30765473647` stays red on `main` forever. It is the last run
of a workflow that is now disabled and will never run again, and the repo is being archived
rather than repaired — that red run is the recorded state of a retired repo, not an open
failure. Nothing was deleted, skipped or relaxed to change it.

`git status` in `C:/Code/KataFlow-ai` after the push: clean, `main` level with `origin/main`.

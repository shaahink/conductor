# N1.1 — the four repos each have a branch off the Node 20 runtime, CI green on the pull request

Session 8. Every version below was read from the action's own latest GitHub release, not guessed.

## Current majors, measured 2026-08-03

| action | latest release | major used |
| --- | --- | --- |
| `actions/checkout` | v7.0.1 | v7 |
| `actions/setup-node` | v7.0.0 | v7 |
| `actions/setup-dotnet` | v6.0.0 | v6 |
| `actions/setup-go` | v7.0.0 | v7 |
| `actions/upload-artifact` | v7.0.1 | v7 |
| `actions/download-artifact` | v8.0.1 | v8 |
| `pnpm/action-setup` | v6.0.10 | v6 |
| `softprops/action-gh-release` | v3.0.2 | v3 |

The artifact pair reads as a mismatch and is not: upload `v7.0.0` and download `v8.0.0` were
published on the same day (2026-02-26) and are the matched set.

No commit-SHA pinning. The fleet does not pin today and changing that convention is out of scope.

## The four branches and their pull-request runs

| repo | branch | PR | PR run | conclusion |
| --- | --- | --- | --- | --- |
| blog-code | `chore/node20-action-bump` | [#1](https://github.com/shaahink/blog-code/pull/1) | `30828584094` (build) | success |
| sitekit | `chore/node20-action-bump` | [#2](https://github.com/shaahink/sitekit/pull/2) | `30828661891` (CI) | success |
| DevContext2 | `chore/node20-action-bump` | [#11](https://github.com/shaahink/DevContext2/pull/11) | `30828680117` (CI) | success |
| site-template | `chore/node20-action-bump` | [#32](https://github.com/shaahink/site-template/pull/32) | `30828982820` (CI) | success |

DevContext2's base is `develop`, not `main` — that repo's default branch is `develop`.

## What actually moved, per repo

- **blog-code** `build.yml`: checkout v5→v7, setup-dotnet v4→v6, setup-go v5→v7.
- **sitekit** `ci.yml` + `release.yml`: checkout v5→v7, setup-node v5→v7.
- **DevContext2** `ci.yml` + `eval.yml` + `release.yml`: checkout v4→v7, setup-dotnet v4→v6,
  setup-node v4→v7, pnpm/action-setup v4→v6, upload-artifact v4→v7, download-artifact v4→v8,
  softprops/action-gh-release v2→v3.
- **site-template**: nothing to bump. The only `uses:` in its `ci.yml` is the call to the
  reusable `site-ci.yml` in `shaahink/.github`, which is already on current majors. What landed
  instead is a `workflow_dispatch` trigger, so the shared pipeline can be re-proven from `main`
  on demand — see N1.3.

## The warnings measured gone, not assumed gone

Read from the check-run annotations API on the old run and the new one.

- **blog-code** old `main` run `30765475178` carried:
  *"Node.js 20 is deprecated … actions/setup-dotnet@v4, actions/setup-go@v5"* (two jobs).
  New PR run `30828584094`: **zero annotations**.
- **DevContext2** old `develop` run `30765480950` carried:
  *"… actions/checkout@v4, actions/setup-dotnet@v4"* and
  *"… actions/checkout@v4, actions/setup-node@v4, pnpm/action-setup@v4"*.
  New PR run `30828680117`: **zero annotations**.
- **sitekit** old `main` run `30645090424`: **already zero annotations** at v5. sitekit was never
  on the Node 20 runtime; its v5→v7 move is currency, not a fix. Recorded as such rather than
  claimed as a warning cleared.

Nothing was skipped, relaxed, deleted or softened to reach any of these greens.

# K1.3 — retirement notice at the top of KataFlow's README

Repo: `shaahink/KataFlow`. Captured 2026-08-03 against the real remote with the `gh` CLI.

## Acceptance

The README carries a short retirement notice at the top saying the repo is archived and
why, committed to `main`.

## What was done

Commit **`af8930a`** on `main` (`docs: retirement notice at the top of the README`),
pushed `f47e28d..af8930a  main -> main`. One file changed, 12 insertions, nothing removed.

The notice sits immediately under the `# KataFlow` heading, above the project description,
and states: the repo is archived and unmaintained; the reason (NuGet restore fails under
audit-as-error on `Microsoft.OpenApi` 2.0.0, GHSA-v5pm-xwqc-g5wc, and `MessagePack` 2.5.192,
both reaching the build transitively through the Aspire AppHost, and the repair was judged
not worth the effort for a finished kata project); the date; and what was done about it —
CI disabled, Dependabot off, open dependency pull requests closed unmerged. It also warns
that the code will not build as-is without pinning or overriding those packages.

## Evidence

`gh api repos/shaahink/KataFlow/contents/README.md`, decoded — the notice is present on
the remote default branch, not just locally:

```
# KataFlow

> **⚠️ This repository is archived and no longer maintained.**
>
> KataFlow was a finished kata project. Its NuGet restore fails under audit-as-error on two
> vulnerable packages — `Microsoft.OpenApi` 2.0.0 (GHSA-v5pm-xwqc-g5wc) and `MessagePack`
> 2.5.192 — which reach the build transitively through the Aspire AppHost. Repairing that
> chain was judged not worth the effort for a project that is complete, so the repo was
> retired on 2026-08-03 instead: CI is disabled, Dependabot is switched off, and the open
> dependency pull requests were closed unmerged.
>
> The code stays here, read-only, for reference. It will not build as-is without pinning or
> overriding those packages yourself, and nothing below is being kept up to date.
```

`gh run list --branch main --limit 3` after the push — still no new run, the disabled `CI`
workflow did not fire:

```
30765473647  CI                                completed  failure  2026-08-02T20:22:37Z
28334313498  nuget in /. - Update #1437613018  completed  success  2026-06-28T20:02:46Z
27915882794  nuget in /. - Update #1426807036  completed  success  2026-06-21T20:02:35Z
```

`git status --short --branch` in `C:/Code/KataFlow-ai`: `## main...origin/main` — clean and
level. This matters for what comes next: K1.4 archives the repo and makes it read-only, so
every commit had to be pushed first.

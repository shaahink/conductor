# Complexity budget for Conductor.Courier (KS6.3). Read by CA1502 / CA1505 / CA1506 through <AdditionalFiles>
# in Directory.Build.props; the three rules are set to error in .editorconfig, so these numbers fail
# the build, they do not advise it.
#
# THE NUMBERS ARE THIS PROJECT'S MEASURED WORST, NOT A ROUND NUMBER SOMEBODY LIKED. Every budget here
# was taken from a build of this tree with the thresholds wound down, and set to the extreme that came
# back. The tree is therefore exactly AT its bar: one more branch in the worst method turns the build
# red. That is the ratchet, and tools/gates/complexity-budget.ps1 is what stops the number going the
# other way - it may only ever come DOWN.
#
# THE GRAMMAR IS NOT FORGIVING AND IT FAILS SILENT. Measured 2026-08-19: one line the analyzer cannot
# parse - a symbol NAME in the parentheses, a typo, a stray word - disables EVERY code-metrics rule in
# this compilation with no diagnostic at all. Not AD0001, not CA1509 (enabled at error and still silent).
# Only 'RuleId: N' and 'RuleId(SymbolKind): N' parse. complexity-budget.ps1 validates every line against
# that grammar and KS6_3ComplexityBudgetTests compiles a canary to prove the rules are still LIVE.
#
# WHY THIS FILE ARRIVED LATE. PK1.1 made the courier its own project and no budget came with it, so the
# gate read the analyzer defaults - looser than every budget in this repo - and refused the omission the
# way it refuses a deletion. It went unseen for five stages because the ratchet is a CI-only step
# (.github/workflows/ci.yml, tools/gates/ratchet.ps1) and no conductor battery runs it: the run
# confirmed PK1-PK5 green against a branch whose CI was red the whole time. Measured 2026-09-18.
#
# The two named surfaces are both composition roots, which is where a courier's coupling belongs:
# CA1506 = 60 is CourierProgram, the entry point that wires the daemon, the listener and the journal;
# CourierListener is the next one down at 51. CA1502 = 12 and CA1505 = 42 are two different methods -
# CourierListener.HandleAsync branches the most, CourierProgram.RunAsync scores the worst composite.
# CA1502 - cyclomatic complexity, per method. Analyzer default: 25.
CA1502: 12
# CA1505 - maintainability index (0-100, HIGHER is better); fires BELOW this. Analyzer default: 10.
CA1505: 42
# CA1506 - class coupling: distinct types a type or member depends on. Analyzer default: 95.
CA1506: 60

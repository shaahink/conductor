# B11 — Close-out + Shamshir owner-gated proof

**Stage goal:** cross-platform gates, real packaging, ADRs/clean-clone battery, and the hard
acceptance test — drive a full owner-gated Shamshir phase.

**Sessions:** ~2. **Persona:** docs. **Depends on:** B1–B10. **Branch:** `feat/baton-b11`.

## Requirements
- R11.1 Cross-platform gate runner: `gates[].shell` ∈ `powershell|bash|sh`; auto-detect default by
  OS. PowerShell path unchanged.
- R11.2 `dotnet tool` packaging (`PackAsTool`), tab completion, `conductor doctor` (prints exactly
  what will happen on resume: pending fix/resume/phase-gate/audit/owner-gate).
- R11.3 ADRs finalised; `StateCompat` + clean-clone battery (clone the repo fresh, run the plan's
  gates, prove no hidden local state).
- R11.4 **Acceptance:** drive a full **owner-gated** Shamshir phase (parity-pipeline **P2.2** — the
  credentialed compare-both headline gate) through Conductor: owner-gate blocks, approval resumes,
  reconcile verdict committed.

## Tasks → checkpoints
- **B11.1** — cross-platform gate runner.
  - Gate: a `bash` gate and a `powershell` gate both run with correct exit-code capture (test on the
    available shell; the other path unit-tested via the abstraction).
- **B11.2** — dotnet tool packaging + completion + doctor.
  - Gate: `dotnet pack` produces the tool; `conductor doctor` prints the resume plan (test/snapshot).
- **B11.3** — ADRs + clean-clone battery.
  - Gate: clean-clone battery green; ADRs committed.
- **B11.4** — Shamshir P2.2 owner-gated acceptance.
  - Gate: evidence (`docs/history/baton/audits/B11-shamshir-p2.2.md`) shows the owner-gate blocked, approval
    resumed, and the reconcile verdict landed — driven by Conductor.

## Definition of done
Cross-platform, packaged, documented, clean-clone-proven; a full owner-gated Shamshir phase driven
end-to-end. Full battery green.

## Traps
- P2.2 needs cTrader credentials + an owner approval — this is the owner-gate + credentialed path;
  it will legitimately BLOCK on `AwaitingOwner`. That's success, not failure.
- Packaging must not break the self-hosting story (the stable driver is still a plain exe/tool).

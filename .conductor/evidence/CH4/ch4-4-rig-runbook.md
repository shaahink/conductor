# Aban - the rig era — the era-close

**Generated by `conductor release runbook` at 2026-08-27 03:07:59Z.** Nothing in this document was performed.
Every line below is the output of the probes `conductor release preflight` runs and the planner
`conductor release perform` uses, rendered — so a line that disagrees with the tree means this
document is stale. Regenerate it; never edit it, and never carry a claim forward from the last one.

| | |
| --- | --- |
| repo | `C:/Users/shahi/AppData/Local/Temp/ch42-rig` |
| merge | `master` → `master` |
| release | `v0.1.0` |
| installed engine | `0.5.0+e60ae79c92dc` |

## The verdict

- preconditions — **NOT READY - 2 of 6 red: processes, courier; 1 waiting on the owner: backfill**  (`release preflight` exits 1)
- acts — **OWNER - nothing mechanical was left to do. 5 act(s) are yours and were stopped at: version, split, corpus, reinstall, publish**  (`release perform` exits 2)

Exit **2** is what a finished era-close looks like: nothing red, and the acts that are yours still
standing where they always stand. A **0** would mean this engine believed it had done your job too.

## 1. The preconditions, measured

| | check | what was measured |
| --- | --- | --- |
| **GREEN** | `merge` | master is already master - nothing to fast-forward |
| **GREEN** | `changelog` | CHANGELOG has a 0.1.0 section and changelog-section.sh exits 0 on it (5 lines) |
| **RED** | `processes` | 4 reason(s) a binary swap is unsafe right now |
| **GREEN** | `migration` | no schema skew: tree v15, installed engine 0.5.0 carries the same migrations |
| **RED** | `courier` | the courier would not survive the reinstall: 1 problem(s) |
| **YOURS** | `backfill` | no GitHub destination is configured, so no run can be owed a record |

**changelog** — CHANGELOG has a 0.1.0 section and changelog-section.sh exits 0 on it (5 lines)

- that body is the release notes verbatim - re-read it before tagging
- if it quotes a run total or a dollar figure, re-run `conductor budget` and `conductor money`: it is a dated measurement

**processes** — 4 reason(s) a binary swap is unsafe right now

- pid 5248 C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe <- CONDUCTOR_PID, the run asking this question
- pid 12956 C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe
- pid 33884 C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe
- a run is live in C:\Code\conductor\.conductor (engine pid 5248) — stop it with `conductor pause` or `conductor kill`, then update
- another conductor is running from C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe (pid 33884) — exit it, then update
- another conductor is running from C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe (pid 5248) — exit it, then update
- another conductor is running from C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe (pid 12956) — exit it, then update
- the reinstall belongs AFTER the run has ended - a live engine holding an old image is exactly trap 18's shape

**courier** — the courier would not survive the reinstall: 1 problem(s)

- this repository is not on the courier's allowlist - its notes park in the dead-letter box (`conductor inbox parked`)
- token set, persisted at User scope - 1 chat(s), 4 project(s) allowed
- task registered, running pid 33884
- `tools/install.ps1 stops the courier at step 0 and puts it back on the new engine - re-check 'conductor courier status' after the reinstall`

**backfill** — no GitHub destination is configured, so no run can be owed a record

- set `github.repo` in the plan, or pass --repo, if the board is meant to be mirrored

## 2. What the engine performs

One command, four acts, each landing as its own commit so the era-close is reviewable an act at
a time. Ordered, and it stops at the first refusal rather than performing the rest on a half-done
sequence. Drop `--yes` to rehearse it.

```
conductor release perform --tag 0.1.0 --yes
```

### changelog — **already true**

CHANGELOG already has a [0.1.0] section

- nothing to rename - this act is idempotent

### merge — **already true**

master already contains every commit of master

- nothing to fast-forward - this act is idempotent, and it stays so after the acts below move the base on

### tag — **already true**

v0.1.0 already exists

- nothing to tag - this act is idempotent, and it will not move an existing tag

### docmove — **already true**

nothing left to move - the plan already points into the record

- docs/history/ABAN-PLAN-2026-08-27.md  (already in the record)
- docs/history/archive/trackers/ABAN-TRACKER.md  (already in the record)

## 3. What is yours

Named on every run, whatever the state, each with the command you type. None of these is ever
reported as done or as nothing: an act that needs a person cannot be allowed to read like one that
has been handled. That mistake is the whole reason this document is generated.

### version — **YOURS**

the release is named 0.1.0 because you said so

- MinVer derives a build id, not a release name; nothing in this repo can decide which number an era is
- everything mechanical above takes it as input - `--tag <x.y.z>`

### split — **YOURS**

one release or two is a call about what the world reads, not about the tree

- a single section covering both eras is one rename; splitting means tagging an intermediate commit
- and cutting one section into two by hand. History usually makes one branch much cheaper - it does not make it right

### corpus — **YOURS**

no run is owed a GitHub record

- whether a run joins the published corpus is a decision about what the world sees, so this engine will not take it
- run each backfill ONCE - a second pass inside GitHub's replica lag mints the board again (bug #79)

### reinstall — **YOURS**

the reinstall cannot happen yet - a conductor is live on this machine

- `tools/install.ps1, then 'conductor version' to confirm it matches the tag`
- re-check the process table at the moment you type it: another repository's run may have started since

### publish — **YOURS**

pushing is what makes any of this public

- `git push origin master`
- `git push origin v0.1.0   # this is what starts the release build across five platforms`
- the merge and the tag above are LOCAL; nothing this engine did has left the machine

## 4. The order

1. `conductor release perform --tag 0.1.0 --yes` — changelog, merge, tag, docmove
2. **version** — the release is named 0.1.0 because you said so
3. **split** — one release or two is a call about what the world reads, not about the tree
4. **corpus** — no run is owed a GitHub record
5. **reinstall** — the reinstall cannot happen yet - a conductor is live on this machine  ·  `tools/install.ps1, then 'conductor version' to confirm it matches the tag`
6. **publish** — pushing is what makes any of this public  ·  `git push origin master`

---

Nothing was merged, tagged, moved, installed or pushed to produce this document. `release runbook`
measures and renders; `release perform --yes` is the only verb here that changes the tree, and it
stops at every act above that is yours.

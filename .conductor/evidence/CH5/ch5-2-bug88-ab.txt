# ch5-2 bug 88 A/B - scratch rig, same plan, same --tag 0.6.0, DRY RUN, only CHANGELOG.md differs
# rig: C:\Users\shahi\AppData\Local\Temp\ch52-changelog-ab (own repo, own state dir, github/telegram blocks stripped)
# A = CHANGELOG.md at 35043fe^ (the placeholder), B = CHANGELOG.md at 35043fe (bug 88 fixed)

===== A: BEFORE, exit 1 =====
conductor release perform — ch52 changelog A/B rig
repo: C:/Users/shahi/AppData/Local/Temp/ch52-changelog-ab/repo  ·  release: 
0.6.0  ·  DRY RUN (nothing is written; add --yes to perform)

✗ changelog  mechanical the [Unreleased] section is a placeholder (2 non-blank 
line(s))
             that body becomes the release notes the world reads, verbatim, via 
changelog-section.sh
             writing what an era shipped is not derivable from anything this 
engine measures - it is yours
✗ merge      mechanical not attempted - an earlier act refused or failed
             the era-close is ordered; performing this one on a half-done 
sequence is how a release goes wrong quietly
✗ tag        mechanical not attempted - an earlier act refused or failed
             the era-close is ordered; performing this one on a half-done 
sequence is how a release goes wrong quietly
✗ docmove    mechanical not attempted - an earlier act refused or failed
             the era-close is ordered; performing this one on a half-done 
sequence is how a release goes wrong quietly
? version    owner      the release is named 0.6.0 because you said so
             MinVer derives a build id, not a release name; nothing in this repo
can decide which number an era is
             everything mechanical above takes it as input - `--tag <x.y.z>`
? split      owner      one release or two is a call about what the world reads,
not about the tree
             a single section covering both eras is one rename; splitting means 
tagging an intermediate commit
             and cutting one section into two by hand. History usually makes one
branch much cheaper - it does not make it right
? corpus     owner      no run is owed a GitHub record
             whether a run joins the published corpus is a decision about what 
the world sees, so this engine will not take it
             run each backfill ONCE - a second pass inside GitHub's replica lag 
mints the board again (bug #79)
? reinstall  owner      the reinstall cannot happen yet - a conductor is live on
this machine
             tools/install.ps1, then `conductor version` to confirm it matches 
the tag
             re-check the process table at the moment you type it: another 
repository's run may have started since
? publish    owner      pushing is what makes any of this public
             git push origin master
             git push origin v0.6.0   # this is what starts the release build 
across five platforms
             the merge and the tag above are LOCAL; nothing this engine did has 
left the machine

STOPPED - 4 act(s) refused or failed: changelog, merge, tag, docmove; would 
perform nothing. Nothing after the first refusal was attempted.

===== B: AFTER, exit 1 =====
conductor release perform — ch52 changelog A/B rig
repo: C:/Users/shahi/AppData/Local/Temp/ch52-changelog-ab/repo  ·  release: 
0.6.0  ·  DRY RUN (nothing is written; add --yes to perform)

→ changelog  mechanical rename '## [Unreleased]' to '## [0.6.0] - 2026-08-27' 
over 106 lines
             after this, `sh tools/changelog-section.sh 0.6.0` exits 0 and 
prints that body
✗ merge      mechanical the preflight's merge line is not green: no such branch:
master and HEAD
             nothing was compared - name the branches with --base and --branch
             `conductor release preflight` is the measurement; this act only 
acts on it
✗ tag        mechanical not attempted - an earlier act refused or failed
             the era-close is ordered; performing this one on a half-done 
sequence is how a release goes wrong quietly
✗ docmove    mechanical not attempted - an earlier act refused or failed
             the era-close is ordered; performing this one on a half-done 
sequence is how a release goes wrong quietly
? version    owner      the release is named 0.6.0 because you said so
             MinVer derives a build id, not a release name; nothing in this repo
can decide which number an era is
             everything mechanical above takes it as input - `--tag <x.y.z>`
? split      owner      one release or two is a call about what the world reads,
not about the tree
             a single section covering both eras is one rename; splitting means 
tagging an intermediate commit
             and cutting one section into two by hand. History usually makes one
branch much cheaper - it does not make it right
? corpus     owner      no run is owed a GitHub record
             whether a run joins the published corpus is a decision about what 
the world sees, so this engine will not take it
             run each backfill ONCE - a second pass inside GitHub's replica lag 
mints the board again (bug #79)
? reinstall  owner      the reinstall cannot happen yet - a conductor is live on
this machine
             tools/install.ps1, then `conductor version` to confirm it matches 
the tag
             re-check the process table at the moment you type it: another 
repository's run may have started since
? publish    owner      pushing is what makes any of this public
             git push origin master
             git push origin v0.6.0   # this is what starts the release build 
across five platforms
             the merge and the tag above are LOCAL; nothing this engine did has 
left the machine

STOPPED - 3 act(s) refused or failed: merge, tag, docmove; would perform 
changelog. Nothing after the first refusal was attempted.


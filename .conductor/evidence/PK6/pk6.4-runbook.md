# Peyk courier - the courier stands on its own — the era-close

**Generated by `conductor release runbook` at 2026-09-18 12:06:09Z.** Nothing in this document was performed.
Every line below is the output of the probes `conductor release preflight` runs and the planner
`conductor release perform` uses, rendered — so a line that disagrees with the tree means this
document is stale. Regenerate it; never edit it, and never carry a claim forward from the last one.

| | |
| --- | --- |
| repo | `C:\Code\conductor` |
| merge | `feat/peyk-courier` → `master` |
| release | `v0.6.0` |
| installed engine | `0.5.1-alpha.0.43+b12540557ade` |

## The verdict

- preconditions — **NOT READY - 4 of 7 red: merge, changelog, docs, processes; 1 waiting on the owner: backfill**  (`release preflight` exits 1)
- acts — **STOPPED - 3 act(s) refused or failed: merge, tag, docmove; would perform changelog, docs. Nothing after the first refusal was attempted.**  (`release perform` exits 1)

Exit **2** is what a finished era-close looks like: nothing red, and the acts that are yours still
standing where they always stand. A **0** would mean this engine believed it had done your job too.

## 1. The preconditions, measured

| | check | what was measured |
| --- | --- | --- |
| **RED** | `merge` | feat/peyk-courier is 69 ahead of master and would fast-forward, but the working tree is not clean |
| **RED** | `changelog` | no CHANGELOG section for 0.6.0 |
| **RED** | `docs` | 11 docs row(s) say 'not in the released binary yet' and become false at the tag |
| **RED** | `processes` | 3 reason(s) a binary swap is unsafe right now |
| **GREEN** | `migration` | no schema skew: tree v15, installed engine 0.5.1-alpha.0.43 carries the same migrations |
| **GREEN** | `courier` | the courier is installed, running and reachable, and its token is persisted where the task can see it |
| **YOURS** | `backfill` | 4 finished run(s) have no GitHub record - whether they join the published corpus is yours |

**merge** — feat/peyk-courier is 69 ahead of master and would fast-forward, but the working tree is not clean

- the working tree has uncommitted changes - commit or stash first, or the merge takes them along
- origin/master is read as of your last fetch - this verb does not fetch, and a stale remote ref is a stale verdict

**changelog** — no CHANGELOG section for 0.6.0

- exit 1 - release.yml runs this as the first job of a tag build, so the tag would be refused
- `conductor release perform --tag 0.6.0` renames '## [Unreleased]' to '## [0.6.0] - <date>' (and refuses if the body is a placeholder); re-run this preflight after it
- sections found: ## [Unreleased], ## [0.5.0] - 2026-08-26, ## [0.4.1] - 2026-08-15, ## [0.4.0] - 2026-08-05, ## [0.3.0] - 2026-08-01, ## [0.2.2] - 2026-08-01
- changelog-section: no section for 0.6.0 in CHANGELOG.md.
  Expected a heading '## [0.6.0] - <date>' with at least one line under it.
  Sections found:
## [Unreleased]
## [0.5.0] - 2026-08-26
## [0.4.1] - 2026-08-15
## [0.4.0] - 2026-08-05
## [0.3.0] - 2026-08-01
## [0.2.2] - 2026-08-01
## [0.2.1] - 2026-08-01
## [0.2.0] - 2026-07-31
## [0.1.0] - 2026-07-28

**docs** — 11 docs row(s) say 'not in the released binary yet' and become false at the tag

- docs/cli.md:150
- docs/cli.md:151
- docs/cli.md:152
- docs/cli.md:393
- docs/cli.md:453
- docs/operating.md:76
- docs/operating.md:77
- docs/operating.md:78
- ... and 3 more
- `conductor release perform --tag 0.6.0` rewrites each to 'New in `v0.6.0`' as its own commit (the docs act); tools/ch3/docs-surface-diff.py exits 1 against the installed binary until then, by design

**processes** — 3 reason(s) a binary swap is unsafe right now

- pid 15672 C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe <- CONDUCTOR_PID, the run asking this question
- pid 25164 C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe
- a run is live in C:\Code\conductor\.conductor (engine pid 15672) — stop it with `conductor pause` or `conductor kill`, then update
- another conductor is running from C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe (pid 15672) — exit it, then update
- another conductor is running from C:\Users\shahi\AppData\Local\Programs\conductor\conductor.exe (pid 25164) — exit it, then update
- the reinstall belongs AFTER the run has ended - a live engine holding an old image is exactly trap 18's shape

**migration** — no schema skew: tree v15, installed engine 0.5.1-alpha.0.43 carries the same migrations

- store: C:\Users\shahi\AppData\Local\conductor\runs\conductor-karvansara-core---the-open-door-308cfb9b\run.db at schema 15

**courier** — the courier is installed, running and reachable, and its token is persisted where the task can see it

- token set, persisted at User scope - 3 chat(s), 6 project(s) allowed
- task registered, running pid 20860
- the courier runs C:\Users\shahi\AppData\Local\Programs\conductor\courier\conductor-courier.exe from its own directory - tools/install.ps1 publishes the engine without stopping it; `tools/install.ps1 -CourierOnly` replaces the courier (one restart)

**backfill** — 4 finished run(s) have no GitHub record - whether they join the published corpus is yours

- f85c0bd7e8e1 Peyk courier - the courier stands on its own - still running; its own backfill is the closing act, not something owed yet
- 9491891fe700463ba0d876c06280cce2 - Karvansara edge - gates that can't be gamed, and the courier (needs_human) has 0 issues on shaahink/conductor
- conductor github sync --backfill 9491891fe700463ba0d876c06280cce2 --dry-run   # then once, for real, without --dry-run
- 9647f1b80d1841e9997a801562a267c7 - Karvansara core - the open door (Aborted) has 0 issues on shaahink/conductor
- conductor github sync --backfill 9647f1b80d1841e9997a801562a267c7 --dry-run   # then once, for real, without --dry-run
- 8cefa5de8f164848bd42b275e14ba9cf - Sarban face - the watcher and the surfaces (Completed) has 0 issues on shaahink/conductor
- conductor github sync --backfill 8cefa5de8f164848bd42b275e14ba9cf --dry-run   # then once, for real, without --dry-run
- e9e21d10aedf4390a1580ac6930bac3e - Sarban core - the engine says what it knows (Completed) has 0 issues on shaahink/conductor
- conductor github sync --backfill e9e21d10aedf4390a1580ac6930bac3e --dry-run   # then once, for real, without --dry-run
- pass the FULL run id: several eras share one store directory and a slug cannot pick between them
- run it ONCE - a second pass inside GitHub's replica lag mints the board again (bug #79)

## 2. What the engine performs

One command, five acts, each landing as its own commit so the era-close is reviewable an act at
a time. Ordered, and it stops at the first refusal rather than performing the rest on a half-done
sequence. Drop `--yes` to rehearse it.

```
conductor release perform --tag 0.6.0 --yes
```

### changelog — **will run**

rename '## [Unreleased]' to '## [0.6.0] - 2026-09-18' over 190 lines

- after this, `sh tools/changelog-section.sh 0.6.0` exits 0 and prints that body

### docs — **will run**

rewrite 11 row(s) in 2 file(s) to name v0.6.0

- docs/cli.md:150
- docs/cli.md:151
- docs/cli.md:152
- docs/cli.md:393
- docs/cli.md:453
- docs/operating.md:76
- docs/operating.md:77
- docs/operating.md:78
- each 'New since `v<prev>`; not in the released binary yet' becomes 'New in `v0.6.0`'; the rest of the row is untouched

### merge — **REFUSED**

the preflight's merge line is not green: feat/peyk-courier is 69 ahead of master and would fast-forward, but the working tree is not clean

- the working tree has uncommitted changes - commit or stash first, or the merge takes them along
- origin/master is read as of your last fetch - this verb does not fetch, and a stale remote ref is a stale verdict
- `conductor release preflight` is the measurement; this act only acts on it

### tag — **REFUSED**

not attempted - an earlier act refused or failed

- the era-close is ordered; performing this one on a half-done sequence is how a release goes wrong quietly

### docmove — **REFUSED**

not attempted - an earlier act refused or failed

- the era-close is ordered; performing this one on a half-done sequence is how a release goes wrong quietly

## 3. What is yours

Named on every run, whatever the state, each with the command you type. None of these is ever
reported as done or as nothing: an act that needs a person cannot be allowed to read like one that
has been handled. That mistake is the whole reason this document is generated.

### version — **YOURS**

the release is named 0.6.0 because you said so

- MinVer derives a build id, not a release name; nothing in this repo can decide which number an era is
- everything mechanical above takes it as input - `--tag <x.y.z>`

### split — **YOURS**

one release or two is a call about what the world reads, not about the tree

- a single section covering both eras is one rename; splitting means tagging an intermediate commit
- and cutting one section into two by hand. History usually makes one branch much cheaper - it does not make it right

### corpus — **YOURS**

4 run(s) have no GitHub record

- conductor github sync --backfill 9491891fe700463ba0d876c06280cce2 --dry-run   # then once, for real
- conductor github sync --backfill 9647f1b80d1841e9997a801562a267c7 --dry-run   # then once, for real
- conductor github sync --backfill 8cefa5de8f164848bd42b275e14ba9cf --dry-run   # then once, for real
- conductor github sync --backfill e9e21d10aedf4390a1580ac6930bac3e --dry-run   # then once, for real
- whether a run joins the published corpus is a decision about what the world sees, so this engine will not take it
- a second pass is safe: the backfill remembers what it created under the state home's github-maps/ (bug #79)

### reinstall — **YOURS**

the reinstall cannot happen yet - a conductor is live on this machine

- tools/install.ps1, then `conductor version` to confirm it matches the tag
- re-check the process table at the moment you type it: another repository's run may have started since

### publish — **YOURS**

pushing is what makes any of this public

- `git push origin master`
- `git push origin v0.6.0   # this is what starts the release build across five platforms`
- the merge and the tag above are LOCAL; nothing this engine did has left the machine

## 4. The order

1. `conductor release perform --tag 0.6.0 --yes` — changelog, docs, merge, tag, docmove
2. **version** — the release is named 0.6.0 because you said so
3. **split** — one release or two is a call about what the world reads, not about the tree
4. **corpus** — 4 run(s) have no GitHub record
5. **reinstall** — the reinstall cannot happen yet - a conductor is live on this machine
6. **publish** — pushing is what makes any of this public  ·  `git push origin master`

---

Nothing was merged, tagged, moved, installed or pushed to produce this document. `release runbook`
measures and renders; `release perform --yes` is the only verb here that changes the tree, and it
stops at every act above that is yours.

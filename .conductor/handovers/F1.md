# F1 Phase Handover — run.db task store + tracker-as-view + task/note verbs + report --query

**Audit sessions:** 2026-07-10 (3 audits)
**Final verdict:** PASS — GENUINELY CORRECT. No new bugs found in third audit; all prior fixes verified.

## What was delivered (F1.1–F1.4)

### F1.1: run.db SQLite schema (`src/Conductor/Core/RunDb.cs`)
- 12 tables auto-created idempotently on open, WAL mode enabled.
- Schema v2 with v1→v2 migration path for checkpoints.
- Best-effort writes (SqliteException logged, never thrown).
- Integrated at 6 event points in Orchestrator.
- Evidence: `tests/Conductor.Tests/RunDbTests.cs` — 12 tests.

### F1.2: Tracker-as-view (`src/Conductor/Core/TrackerGenerator.cs`)
- Generates TRACKER.md as a projection FROM run.db + PlanConfig.
- Byte-stable output for identical inputs.
- Orchestrator seeds checkpoints on first run, regenerates tracker after every session.

### F1.3: task/note verbs + MCP surface
- `conductor task --list | --done <id> | --in-progress <id>` via run.db.
- `conductor note -k <kind> -s <stage> <text>` writes to ledger table.
- `conductor_note` MCP tool in McpTaskServer with ledger write + journal fallback.

### F1.4: report --query
- `conductor report --query "<SQL>"` runs parameterized SQL against run.db.

## What the first audit session (58f3ba4) FIXED

1. RunDb constructor resource leak (connection not closed on schema creation failure).
2. SQLite WAL mode (PRAGMA journal_mode=WAL).
3. TrackerGenerator dead variable + null-safe StatusLabel.
4. TaskCommand defensive error handling around GetCheckpoints.

## What THIS audit session FIXED (second audit)

1. **RunDb.Dispose idempotency** (`RunDb.cs:561`): `Dispose()` was not safe for multiple calls — calling it twice would throw ObjectDisposedException from `_conn.Close()`. Added `_disposed` flag with `Interlocked.CompareExchange` so double-dispose is a no-op. This matters because CLI commands that wrap RunDb in `using` + `using` (or the orchestrator disposing during shutdown while a finally block also disposes) would crash.

2. **CLI RunDb construction error handling** (`Commands.cs` — ReportCommand.RunQuery, TaskCommand.Execute, NoteCommand.OpenRunDb): The `new RunDb(path, logger)` constructor opens the connection and calls `EnsureSchema()` which can throw `InvalidOperationException` (schema version too new) or `SqliteException` (corrupted DB). These were previously unhandled in three CLI verbs, crashing the CLI with an unhandled exception dump. All three now wrap construction + usage in a single try-catch that surfaces a clean error message.

3. **TrackerGenerator hardcoded values** (`TrackerGenerator.cs:49-58, 125-133`): Removed the hardcoded "net10.0" framework version and "548 pass (0w/0e)" test count from the "Baseline numbers" section — these were literal placeholders, not derived from any data source. The section now shows only DB-derivable values (checkpoint total + done count). The hardcoded dependency graph (`F0 → F1 → (F2, F3) → F4` etc.) was replaced with plan-derived dependencies from `plan.Stages[].DependsOn`.

## What remains weak / shortcut / deferred

| # | Item | Severity | Followup |
|---|---|---|---|
| D1 | **TrackerGenerator no test/framework baseline** (`TrackerGenerator.cs:49`): Test count and framework version are no longer rendered at all (removed as misleading). If they're needed, they should come from gate battery results (test counts) and the project's `TargetFramework` attribute. The scores table in run.db exists but is unused (F4 will populate it). | LOW | Track as FU-F1-01. Revisit in F2/F4. |
| D2 | **McpTaskServer.HandleNote uses TaskAdded as journal container** (`McpTaskServer.cs:286`): Notes persist as `TaskAdded` events with checkpointId="ledger", polluting the task graph. A dedicated `NoteAdded` event would be cleaner. | MEDIUM | Primary path writes to run.db ledger table directly. The journal is the fallback. Track as FU-F1-02. |
| D3 | **SeedCheckpointsFromTracker dual parse path** (`Orchestrator.cs:2491`): Seed reads old tracker via MarkdownTableProvider, then regenerates the tracker which parses the same format. Migrating to pure DB-first seeding (checkpoints defined in plan JSON) would eliminate the parse round-trip. | LOW | Design decision for F2. Current approach is additive-first, zero-regression. |
| D4 | **No concurrent-write guard on RunDb** (`RunDb.cs:12`): Relies on single-threaded orchestrator loop. CLI commands open separate instances (correct). If parallel writes are introduced, WAL mode handles concurrent-readers but still only one writer. | LOW | Acceptable for current architecture. Revisit in F2.1. |
| D5 | **EmitSessionFinished commit SHA extraction** (`Orchestrator.cs:1902`): `rec.NewCommits[^1].Split(' ')[0]` assumes git log format. | LOW | Advisory-only evidence. Track as FU-F1-03. |
| D6 | **McpTaskServer.HandleNote empty catch** (`McpTaskServer.cs:280`): Swallows DB write failures silently. Journal write serves as durable fallback. | LOW | Adding an ILogger requires DI changes to McpTaskServer — deferred. |

## Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit/note |
|---|---|---|---|
| RunDb.Dispose not idempotent (ObjectDisposedException on double-dispose) | MEDIUM | YES | This session |
| CLI RunDb construction exceptions unhandled in 3 verbs | MEDIUM | YES | This session |
| TrackerGenerator hardcoded baseline values (framework, test count) | LOW | YES | This session — removed misleading placeholders |
| TrackerGenerator hardcoded dependency graph | LOW | YES | This session — derived from plan data |

## Gate battery
- Build: 0 warnings, 0 errors
- Tests: 548/548 pass, 0 skipped
- Format analyzer: 0 issues (warnings-as-errors; no suppressed rules lowered)

## Risks for next phase (F2)
1. Verifier scoring loop (F4) needs `scores` table — schema ready, unused.
2. Lane pool workers (F2.1) writing run.db: WAL mode handles single-writer, but test under concurrent load.
3. `conductor_note` MCP tool's TaskAdded journal pollution could affect `conductor tasks` output — consider dedicated event before F2 ships.
4. TrackerGenerator dependency graph renders only explicit `DependsOn` edges; sequential ordering (plan.Stages order) is implicit, not rendered.

## Followups created
- `FU-F1-01` through `FU-F1-03` tracked in `.conductor/followups.md`.

---

## Third audit (this session, 2026-07-10) — static-architecture review

**Gate battery re-run:** 548/548 pass, 0w/0e. All gates green.

### Audit scope
Full static review of all 14 files in the `db9e988..HEAD` diff, with particular focus on:
- Async correctness (ConfigureAwait, CancellationToken threading, sync-over-async boundaries)
- Race conditions and resource leaks
- Null safety and edge cases
- SOLID / analyzer cleanliness
- Shallow/stubbed implementations with TODO markers
- Leftover artifacts from delivery sessions

### Findings
**No new correctness bugs found.** The previous two audits (58f3ba4, 542422c) resolved all substantive issues. Specific areas verified clean:

| Area | Verdict |
|---|---|
| `RunDb` constructor fail-safe (Close+Dispose on schema failure) | Correct — connection is always closed on throw |
| `RunDb.Dispose` idempotency | Double-dispose guard via `Interlocked.CompareExchange` — verified |
| `RunDb._initialized` thread safety | Safe — set only in constructor, single-writer |
| `RunDb.TryExecute` exception swallowing | Correct — additive, best-effort pattern; journal fallback exists |
| `Orchestrator` null-safe `_runDb?.` access | All sites use null propagation or `is { }` pattern |
| `EmitGates` — null `g.Tail` | FALSE ALARM — `GateResult` is a positional record with non-nullable `string Tail` |
| `EmitGates` — gate tier resolution via `plan.Gates.FirstOrDefault(gc => gc.Name == g.Name)?.Tier ?? "full"` | Correct — defaults to "full" when gate not in plan config |
| `SeedCheckpointsFromTracker` — `ReadTrackerSafe()` returning empty | Correct — re-seeding is idempotent via ON CONFLICT, DB is source of truth |
| `RegenerateTracker` — file write atomicity | Correct — `File.WriteAllText` uses temp+rename on Windows |
| `TrackerGenerator.Generate` — null/empty plan.Stages | Safe — empty loop, empty deps |
| `TrackerGenerator.Generate` — `PromptExtra` vs `PlanDoc` condition | Cosmetic only — condition gates header detail rendering, not correctness |
| `ReportCommand.RunQuery` — RunDb opening | Safe — idempotent schema creation, parameterised queries |
| `NoteCommand` / `TaskCommand` — RunDb construction failure | Wrapped in try-catch with clean CLI error messages |
| `McpServeCommand` — RunDb wiring | Best-effort with catch — MCP works without run.db |
| `McpTaskServer.HandleNote` empty catch | Best-effort — journal write is durable fallback; DI logger change deferred (D6) |
| `ConductorHost.Build` — RunDb dry-run null propagation | Correct — `return null!` for dry runs, `sp.GetService<RunDb>()` returns null |
| Async flow — `ConfigureAwait(false)` threading | All async methods in Orchestrator use it; sync boundary at Spectre.Cli is correct |
| No `async void` | No occurrences in the diff |
| No blocking `.Result`/`.Wait()` except at Spectre.Cli boundary | Suppressed with MA0045 pragma + rationale comment |
| No TODOs in phase diff | Zero TODO markers in all 14 files |

### What remains weak/deferred
All 6 deferred items from the second audit remain valid and unchanged:
1. **D1 / FU-F1-01** (LOW): TrackerGenerator baseline numbers (framework/test) — design decision for F2/F4
2. **D2 / FU-F1-02** (MEDIUM): McpTaskServer.HandleNote uses TaskAdded as journal container — deferred (dedicated event type needs cross-cutting change)
3. **D3 / FU-F1-03** (LOW): SeedCheckpointsFromTracker dual parse path — design decision for F2
4. **D4** (LOW): No concurrent-write guard on RunDb — acceptable for single-threaded orchestrator
5. **D5** (LOW): EmitSessionFinished commit SHA extraction — advisory-only, tracked as FU-F1-03
6. **D6** (LOW): McpTaskServer.HandleNote empty catch — DI logger change deferred

### Risks for F2
- Verifier scoring loop (F4) needs `scores` table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: WAL mode handles single-writer, but test under concurrent load
- `conductor_note` MCP tool's TaskAdded journal pollution could affect `conductor tasks` output
- TrackerGenerator dependency graph renders only explicit `DependsOn` edges; sequential ordering is implicit
- FU-F0-5 (`EventLog.Dispose` blocks synchronously) remains OPEN — F2 is a good stage to resolve it
- FU-F0-4 (`.GetAwaiter().GetResult()` at Spectre.Cli boundaries) remains OPEN — convert to `async Task<int>` when Commands.cs is touched next

---

## Fourth audit (this session, 2026-07-10 18:06 UTC) — static-architecture HARDEN

**Gate battery:** 548/548 pass, build 0w/0e. All gates green.

### Audit scope
Extended the third audit's static review with focused contract verification:
- RunDb.TryExecute exception contract: does "best-effort, never throw" actually hold?
- TrackerGenerator.Generate input precondition: does it survive a null plan.Stages?
- RunDb.Query / GetCheckpoints / GetLatestHandover: downstream hardening beyond the happy path
- RunDb connection lifecycle: is a write after Dispose safe?
- All 14 diff files re-scanned for new edge cases

### What this audit FIXED

| # | Issue | Severity | File | Fix |
|---|-------|----------|------|-----|
| 1 | RunDb.TryExecute caught only SqliteException; ObjectDisposedException and InvalidOperationException (from SqliteConnection.CreateCommand() on a disposed/faulted connection) would bypass the catch and crash the caller — violating the "best-effort, never thrown" contract | MEDIUM | RunDb.cs:555 | Catch widened to exception filter: SqliteException or ObjectDisposedException or InvalidOperationException |
| 2 | TrackerGenerator.Generate iterated plan.Stages at 3 sites without null guard — NRE if PlanConfig.Stages is null (despite validation, defensive coding against serialisation edge cases) | LOW | TrackerGenerator.cs:68,86,125 | Added (plan.Stages ?? []) at all 3 enumeration sites |

### Findings verified clean (re-audited, no new issues)

| Area | Verdict |
|---|---|
| RunDb.SeedCheckpoints ON CONFLICT clause | Correct — only UPDATEs title and stage_id on conflict; status/commit/evidence preserved from prior UpdateCheckpoint calls |
| RunDb.MarkCheckpointInProgress guard clause | Correct — AND status = 'TODO' prevents downgrading DONE checkpoints (test at RunDbTests.cs line 261 confirms) |
| RunDb.Dispose — write-after-dispose safety | Safe — Dispose calls Interlocked.CompareExchange; writes check _runDb? before calling; single-threaded Orchestrator ensures no concurrent write during shutdown |
| RunDb.Query — disposed-connection guard | Missing explicit guard, but all Orchestrator callers (GetCheckpoints, GetLatestHandover) are behind "if (db is { } db)" checks; CLI callers own the instance via using |
| TaskCommand.Execute — RunDb open in using block | Correct — new instance per CLI invocation, disposed at end of Execute |
| NoteCommand.Execute — same pattern | Correct — OpenRunDb helper wraps construction in try-catch |
| ReportCommand.RunQuery — raw SQL injection | Accepted — --query is an operator diagnostic tool on a local DB; audience is the developer, not an untrusted user |
| Orchestrator.SeedCheckpointsFromTracker dual parse path | Correct — additive-first, zero-regression; old tracker file remains canonical until DB is source of truth |
| Orchestrator.RegenerateTracker — handoff read/write ordering | Correct — reads handoff from tracker file (agent-written), persists to DB, then regenerates tracker FROM DB |
| McpServeCommand RunDb wiring | Correct — best-effort open with catch; MCP works without run.db |
| McpTaskServer.HandleNote empty catch to journal fallback | Correct as designed — journal write is the durable fallback; empty catch is documented as FU-F1-02 (D2) |
| Schema version migration (v1 to v2) | Correct — MigrateFrom adds checkpoints table; schema_version updated in same transaction |
| RunDb.CreateSchema SQL interpolation | Accepted — CurrentSchemaVersion is public static readonly int = 2, a compile-time constant; parameterisation would require clearing cmd.Parameters between reused-command calls, adding risk for cosmetic fix |

### What remains weak/deferred
All 6 prior deferred items (D1-D6) remain valid and unchanged. No new deferred items found.

### Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit |
|---|---|---|---|
| RunDb.TryExecute contract violation — ObjectDisposedException leak | MEDIUM | YES | b93865a |
| TrackerGenerator.Generate NRE on null plan.Stages | LOW | YES | b93865a |
| HostLoggingTests.* flaky (file-share race) | LOW | NO — pre-existing FU-F0-6 | N/A |

### Risks for F2 (unchanged from prior audits, plus 1 new)
- Verifier scoring loop (F4) needs scores table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: test under concurrent load with WAL single-writer model
- conductor_note MCP tool's TaskAdded journal pollution
- TrackerGenerator dependency graph renders only explicit DependsOn edges
- FU-F0-5 (EventLog.Dispose synchronous) — F2 is a good stage to resolve
- FU-F0-4 (.GetAwaiter().GetResult() at Spectre.Cli boundaries) — convert to async Task<int>
- **NEW:** RunDb.Query (and callers GetCheckpoints/GetLatestHandover) lack a disposed-connection guard — safe today (single-threaded orchestrator, CLI owns instances) but would fail loudly if concurrent access is added in F2.1. Add a _disposed check + ObjectDisposedException to Query before F2.1 ships.

### Followups
No new followups — existing ones (FU-F1-01 through FU-F1-03, FU-F0-1 through FU-F0-6) remain tracked in .conductor/followups.md.

---

## Fifth audit (this session, 2026-07-10 — static-architecture HARDEN + FINISH)

**Gate battery:** 548/548 pass, build 0w/0e. All gates green.
**Commit:** 60ab247

### Audit scope
Full static review of all 14 files in the `db9e988..HEAD` diff (plus 2 pre-existing `catch` sites with identical bug). Focus: correctness, concurrency, resource leaks, edge cases, exception-safety consistency.

### What this audit FIXED

| # | Issue | Severity | File | Fix |
|---|-------|----------|------|-----|
| 1 | `ReadTrackerSafe` (and 2 pre-existing sites: `PushIdleSnapshot`, `SaveAndReport`) caught only `IOException`/`InvalidOperationException`, missing `UnauthorizedAccessException` (NOT a subclass of `IOException`) and parser exceptions. Orbit crash if tracker file had restrictive permissions. The rest of the codebase (7 other I/O sites) consistently catches both `IOException` and `UnauthorizedAccessException` separately — these 3 new F1 sites were inconsistent. | MEDIUM | Orchestrator.cs:2509, 1849, 2000 | All 3 `catch` filters widened to `catch (Exception)` — the methods are explicitly documented as "defensive / safe" readers and should never throw |

### Findings verified clean (full re-audit, no new issues)

| Area | Verdict |
|---|---|
| RunDb schema creation (transactional atomicity, power-failure recovery) | Correct — `EnsureSchema` wraps `CreateSchema` + version commit in a single transaction; if transaction fails, Rollback ensures no partial schema. On restart, full creation runs again idempotently. |
| RunDb `CreateSchema` version-check early return at `ver == CurrentSchemaVersion` | Correct — the early return is inside the same transaction that committed the full schema. Tables exist because DDL is transactional in SQLite. |
| RunDb.Migration from v1→v2 | Correct — `MigrateFrom` adds checkpoints table; `CREATE TABLE IF NOT EXISTS` is idempotent. Schema version updated in same transaction. |
| RunDb.TryExecute exception contract | Verified — catches `SqliteException`, `ObjectDisposedException`, `InvalidOperationException`. All code paths that could throw on a disposed/faulted connection are covered. |
| RunDb.Dispose idempotency | Verified — `Interlocked.CompareExchange` guard, `_conn.Close()` + `_conn.Dispose()` in try-catch. Write-after-dispose is safe (TryExecute catches the exception). |
| RunDb.Query (read path) disposal guard | Missing explicit guard, but **all callers safe**: Orchestrator uses `_runDb is { } db` pattern (singleton lifecycle), CLI commands use `using` blocks. Risk only if F2.1 adds concurrent access — documented in risks below. |
| RunDb DI registration in ConductorHost | Correct — dry-run path returns `null!` (RunDb is nullable in Orchestrator ctor), live path creates instance. Orchestrator resolves via `sp.GetService<RunDb>()` (nullable). |
| Orchestrator `_runDb?.` null propagation | All 12 DB-write sites use `?.` or `is { } db` pattern. No NRE risk. |
| `SeedCheckpointsFromTracker` — called with null RunDb (dry-run) | Correct — `if (_runDb is not { } db) return;` guard prevents null deref AND prevents unnecessary file I/O in dry runs. |
| `EmitSessionFinished` RunDb writes ordering | Correct — handoff persisted to DB, THEN tracker regenerated from DB. Generated tracker always has latest handover. |
| `EmitGates` gate tier resolution | Correct — `plan.Gates.FirstOrDefault(gc => gc.Name == g.Name)?.Tier ?? "full"` defaults to "full" for unknown gates. |
| `EmitGates` tail truncation | Correct — `g.Tail.Length > 2000 ? g.Tail[^2000..] : g.Tail` keeps last 2000 chars. |
| `EmitSessionFinished` commit SHA extraction `rec.NewCommits[^1].Split(' ')[0]` | Guarded by `rec.NewCommits.Count > 0` — falls back to "-" when empty. |
| TrackerGenerator.Generate null-safe `plan.Stages` | All 3 enumeration sites use `plan.Stages ?? []`. |
| TrackerGenerator.StatusLabel null-safe | `(status ?? "").ToUpperInvariant()` handles null input. |
| TrackerGenerator checkpoint ordering in rendered output | Rendering iterates `plan.Stages` in plan-defined order, looks up checkpoints via dictionary. Order is deterministic for same plan + DB state. Extraneous DB-only stages rendered in dictionary order (non-deterministic) — cosmetic edge case, not a bug. |
| TrackerGenerator.Write file atomicity | `File.WriteAllText` on NTFS is effectively atomic for small files (~20KB tracker). On non-Windows, a temp+rename pattern would be safer, but run.db is source of truth — tracker regenerates correctly on resume. Very low risk. |
| ReportCommand.RunQuery (F1.4) SQL injection | Accepted — `--query` is a diagnostic tool on a local DB; audience is the developer. Parameterized queries used internally, but raw SQL from CLI is passed directly. |
| ReportCommand.RunQuery Spectre table rendering | Correct — columns derived from `reader.GetName(i)`, values formatted by type, nulls rendered as `[grey]-[/]`. `Markup.Escape` prevents Spectre markup injection. |
| TaskCommand.Execute RunDb lifecycle | Correct — new `RunDb` instance per CLI invocation in `using` block. |
| NoteCommand.OpenRunDb → NoteCommand.Execute catch wrapping | Redundant double-catch (helper wraps in InvalidOperationException, Execute catches generic Exception) but harmless. Helper exists to share OpenRunDb with other future verbs. |
| McpServeCommand RunDb wiring | Correct — best-effort open with catch; MCP works without run.db. `runDb?.Dispose()` in finally block. |
| McpTaskServer.HandleNote (F1.3) dual-write | Correct — DB write is best-effort (empty catch, documented as D6); journal write is durable fallback. Note survives regardless. |
| `conductor_note` MCP tool registration | Correct — 4th tool added alongside existing 3 tools. Test updated (ToolsList_ReturnsFourTools). |
| RunDbTests (15 tests) | Cover: schema creation (all 12 tables), version check, session/gate/cost/ledger/handover/injection/attempt round-trips, idempotent re-open, parameterised query injection prevention, checkpoint seed/update/in-progress guard. All 15 pass. |
| Async flow across all 14 files | Verified — all `ConfigureAwait(false)` where needed, no `async void`, no blocking `.Result`/`.Wait()` except at Spectre.Cli boundary (MA0045 pragma with rationale). |
| Zero TODOs in diff | Confirmed — no `// TODO` markers in any of the 14 files. |
| Analyzer severity | No rules lowered to pass. Build is 0w/0e under warnings-as-errors. |

### What remains genuinely weak / shortcut / assumed

All 6 prior deferred items (D1–D6) remain valid and unchanged:

| # | Item | Severity | Followup |
|---|---|---|---|
| D1 | TrackerGenerator no test/framework baseline — removed as misleading. If needed, derive from gate results (scores table, F4). | LOW | FU-F1-01 |
| D2 | McpTaskServer.HandleNote uses TaskAdded as journal container (pollutes task graph with checkpointId="ledger"). | MEDIUM | FU-F1-02 |
| D3 | SeedCheckpointsFromTracker dual parse path — reads old tracker, re-seeds. Pure DB-first seeding (checkpoints from plan JSON) would eliminate round-trip. | LOW | Design decision for F2. Current approach is additive-first, zero-regression. |
| D4 | No concurrent-write guard on RunDb — single-threaded orchestrator is sufficient today. WAL mode handles concurrent readers. | LOW | Acceptable for current architecture. Revisit in F2.1 lane pool. |
| D5 | EmitSessionFinished commit SHA extraction (`rec.NewCommits[^1].Split(' ')[0]`) assumes git log format. | LOW | FU-F1-03. Advisory-only evidence field. |
| D6 | McpTaskServer.HandleNote empty catch — swallows DB write failures silently; journal write serves as durable fallback. | LOW | Adding ILogger requires DI changes to McpTaskServer — deferred. |

**New deferred items from this audit:**

| # | Item | Severity | Followup |
|---|---|---|---|
| D7 | RunDb.Query + callers (GetCheckpoints, GetLatestHandover) lack a disposed-connection guard — safe today (single-threaded orchestrator + CLI owns instances) but would fail loudly if F2.1 adds concurrent access. Add `if (_disposed != 0) throw new ObjectDisposedException(...)` to Query before F2.1 ships. | LOW | Track as FU-F1-04. Pre-existing from fourth audit, reconfirmed. |
| D8 | TrackerGenerator.Write uses `File.WriteAllText` which is not temp+rename on Linux (NTFS makes it effectively atomic for small files). On ext4/XFS with power failure, a partial write could corrupt the tracker. run.db is source of truth so the next regeneration fixes it — no data loss, only transient corruption. | VERY LOW | Consider `File.Move` from temp file in F2 for cross-platform safety. |
| D9 | RunDb ctor `catch` block (line 44) only re-throws the original exception after `Close()`+`Dispose()`. If `Dispose()` itself throws, the original exception is masked — classic exception-during-cleanup problem. SqliteConnection.Dispose rarely throws in practice. | VERY LOW | `try { _conn.Dispose(); } catch { }` would be safer but may hide real errors. Current behavior is acceptable. |

### Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit |
|---|---|---|---|
| ReadTrackerSafe + PushIdleSnapshot + SaveAndReport narrow exception filter (missing UnauthorizedAccessException, parser failures) — inconsistent with rest of codebase, could crash orchestrator on permission-denied tracker files | MEDIUM | YES | 60ab247 |
| HostLoggingTests.DryRunWritesStructuredLogWithRunIdCorrelation flaky (file-lock race) | LOW | NO — pre-existing FU-F0-6 | N/A |

### Risks for F2 (cumulative from all audits)

- Verifier scoring loop (F4) needs `scores` table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: WAL mode handles single-writer, test under concurrent load
- `conductor_note` MCP tool's TaskAdded journal pollution could affect `conductor tasks` output
- TrackerGenerator dependency graph renders only explicit `DependsOn` edges; sequential ordering is implicit
- FU-F0-5 (EventLog.Dispose synchronous) — F2 is a good stage to resolve
- FU-F0-4 (.GetAwaiter().GetResult() at Spectre.Cli boundaries) — convert to async Task<int>
- D7: RunDb.Query disposed-connection guard — add before F2.1 concurrent writes ship
- D8: TrackerGenerator.Write cross-platform atomicity — low priority

### Evidence paths
- Schema: `src/Conductor/Core/RunDb.cs:29-284` (construction + schema), `tests/Conductor.Tests/RunDbTests.cs` (15 tests)
- Tracker-as-view: `src/Conductor/Core/TrackerGenerator.cs:11-160`, `src/Conductor/Core/Orchestrator.cs:2488-2525`
- task/note verbs: `src/Conductor/Commands/Commands.cs:1646-1813`
- MCP surface: `src/Conductor/Core/Integrations/McpTaskServer.cs:255-295`, `src/Conductor/Commands/Commands.cs:1483-1514`
- report --query: `src/Conductor/Commands/Commands.cs:586-662`
- DI wiring: `src/Conductor/Core/Hosting/ConductorHost.cs:103-123`
- Exception-safety fix: `src/Conductor/Core/Orchestrator.cs:1849,2000,2510` (this session)

---

## Sixth audit (this session, 2026-07-10) — static-architecture HARDEN + FINISH

**Gate battery:** 547/547 pass (1 pre-existing flaky FU-F0-6 excluded), build 0w/0e. All gates green.

### Audit scope
Full independent re-audit of all 14 files in `db9e988..HEAD`. Focus areas distinct from prior audits:
- `EnsureSchema` transaction rollback completeness across exception types
- `SeedCheckpoints` partial-failure semantics (no transaction wrapping)
- `RunDb.Query` + `TryExecute` disposed-connection safety
- `TrackerGenerator.Generate` byte-stability / non-deterministic iteration
- `EmitSessionFinished` handoff persistence ordering correctness
- Unobserved tasks, CancellationToken threading, async void audit (reconfirmed clean)
- SOLID / single-responsibility review of RunDb (schema + write + query + checkpoint CRUD)

### What this audit FIXED

| # | Issue | Severity | File | Fix |
|---|-------|----------|------|-----|
| 1 | **RunDb.Query lacked a disposed-connection guard** (D7 from fourth audit). If F2.1 adds concurrent lane-pool access and Query is called after Dispose, the error would be an unhelpful low-level SqliteException/InvalidOperationException from `_conn.CreateCommand()`. Added explicit `ObjectDisposedException` throw at entry — makes the error self-documenting and closes a tracked deferred item. | LOW | RunDb.cs:517 | Added `if (_disposed != 0) throw new ObjectDisposedException(nameof(RunDb));` at Query entry |

### Findings verified clean (fresh re-audit, no issues found)

| Area | Verdict |
|---|---|
| `EnsureSchema` narrow catch (`SqliteException` only, line 64) | Correct — non-SqliteException propagates to constructor catch-all which calls Close (implicit rollback). All DDL inside transaction is undone either way. Intentional: SqliteException is the expected DDL error; others are constructor bugs handled by the connection-close safety net. |
| `SeedCheckpoints` no transaction wrapping | Acceptable — each UPSERT is idempotent via `ON CONFLICT`. Partial-failure (some INSERTs succeed, some fail) is theoretically possible but vanishingly unlikely on local SQLite. Even if it occurs, `SeedCheckpointsFromTracker` reads the tracker FILE (agent-authored, intact) on restart and re-seeds. The tracker file is only regenerated AFTER checkpoints are written to DB, so crash between write+regenerate doesn't lose data. Loop-per-row is the cost of best-effort `TryExecute` per row. |
| `SeedCheckpoints` ON CONFLICT semantics | Correct — updates only `title` and `stage_id` on conflict; `status`/`commit`/`evidence` are preserved from `UpdateCheckpoint`. Verified in test `SeedCheckpoints_persists_and_re_seeds_idempotently` (line 211). |
| `RunDb.TryExecute` disposed-connection behavior | Correct — catches `ObjectDisposedException` + logs warning. Adding a `if (_disposed != 0) return;` guard would lose the log signal; current catch+log is better for diagnostics. |
| `EmitSessionFinished` handoff persistence ordering (Orchestrator:1908-1914) | Correct — reads tracker (agent-authored handoff), writes to DB, then regenerates tracker from DB. Generated tracker always has latest DB handover (or fallback to agent-written text). |
| `TrackerGenerator.Generate` byte-stability for DB-only stages | Non-issue — `GroupBy` + `ToDictionary` preserves first-occurrence order from `GetCheckpoints`'s `ORDER BY stage_id, id`. Dictionary iteration in .NET is insertion-order-stable in practice. DB-only stages (not in plan) are a rare edge case; their rendering order matches DB query order. |
| `RegenerateTracker` file atomicity on NTFS | `File.WriteAllText` on NTFS uses temp+rename, effectively atomic for small files. Cross-platform concern (D8) remains low priority — run.db is source of truth. |
| `McpTaskServer.RunAsync` stdin loop termination | Correct — null line (EOF) terminates the while loop. CancellationToken check additionally guards the loop. |
| `McpTaskServer.HandleNote` journal write failure propagation | Correct — `WriteJournal` exception propagates through `HandleToolCall` catch-all, which returns an error response to the MCP client. Agent sees the error message. No crash. |
| `McpServeCommand` RunDb disposal | Correct — `runDb?.Dispose()` in finally block after `RunAsync` completes. Singleton managed by DI in Orchestrator path. No leak. |
| `NoteCommand.OpenRunDb` double-catch | Harmless — inner transforms to InvalidOperationException, outer formats for CLI. Redundant but functional. |
| `ConductorHost.Build` RunDb singleton disposal | Correct — registered as singleton; DI container calls Dispose on host shutdown. `return null!` for dry-run means no instance, no disposal call. |
| `ReportCommand.RunQuery` SQL injection | Accepted — diagnostic tool on local DB, operator-audience only. |
| Zero TODOs in diff | Confirmed — no `// TODO`, `// FIXME`, or `// HACK` markers in any of the 14 files. |
| Zero lowered analyzer severities | Confirmed — all suppressions have inline rationale (MA0045 for sync file I/O at Spectre.Cli boundary or local SQLite; CA1031 for best-effort catch). |
| Async flow (all files) | Re-verified: all `ConfigureAwait(false)`, no `async void`, no blocking `.Result`/`.Wait()` except at Spectre.Cli boundary. |

### What remains genuinely weak / shortcut / assumed

All 6 prior deferred items (D1–D6, FU-F1-01–FU-F1-03) and 3 items from prior audits (D7–D9) remain valid. D7 is now FIXED (disposed-connection guard added to Query).

| # | Item | Status | Severity | Followup |
|---|---|---|---|---|
| D1 | TrackerGenerator no test/framework baseline — removed as misleading. If needed, derive from gate results (F4). | DEFERRED | LOW | FU-F1-01 |
| D2 | McpTaskServer.HandleNote uses TaskAdded as journal container (checkpointId="ledger") — pollutes task graph. | DEFERRED | MEDIUM | FU-F1-02 |
| D3 | SeedCheckpointsFromTracker dual parse path (tracker file → DB → regenerate tracker). Pure DB-first seeding would eliminate round-trip. | DEFERRED (design decision for F2) | LOW | — |
| D4 | No concurrent-write guard on RunDb — single-threaded orchestrator is sufficient today. WAL mode handles concurrent readers. | DEFERRED (acceptable for current arch) | LOW | — |
| D5 | EmitSessionFinished commit SHA extraction (`rec.NewCommits[^1].Split(' ')[0]`) assumes git log --oneline format. | DEFERRED | LOW | FU-F1-03 |
| D6 | McpTaskServer.HandleNote empty catch — DB write failures silently swallowed; journal write is durable fallback. | DEFERRED (ILogger DI change) | LOW | — |
| D7 | ~~RunDb.Query lacked disposed-connection guard~~ | **FIXED this session** | — | — |
| D8 | TrackerGenerator.Write uses `File.WriteAllText` (not temp+rename on Linux). run.db is source of truth, so next regeneration fixes corruption. | DEFERRED | VERY LOW | — |
| D9 | RunDb ctor `catch` block — if `Dispose()` throws during cleanup, original exception is masked. SqliteConnection.Dispose rarely throws. | DEFERRED | VERY LOW | — |

**New deferred item from this audit:**

| # | Item | Severity | Followup |
|---|---|---|---|
| D10 | `SeedCheckpoints` does not wrap the upsert loop in a transaction — each row is a separate implicit transaction. If the loop is interrupted mid-way (power failure, process kill), some checkpoints may be seeded and others not. The next `SeedCheckpointsFromTracker` call reads the tracker FILE (intact) and re-seeds, so no permanent data loss. Moving to a single transaction would make the operation atomic. | VERY LOW | FU-F1-05 |

### Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit |
|---|---|---|---|
| RunDb.Query lacked disposed-connection guard (D7) | LOW | YES | this session |
| HostLoggingTests.DryRunWritesStructuredLogWithRunIdCorrelation flaky | LOW | NO — pre-existing FU-F0-6 | N/A |

### Risks for F2 (cumulative + updated)

- Verifier scoring loop (F4) needs `scores` table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: WAL mode handles single-writer, test under concurrent load
- `conductor_note` MCP tool's TaskAdded journal pollution could affect `conductor tasks` output
- TrackerGenerator dependency graph renders only explicit `DependsOn` edges; sequential ordering is implicit
- FU-F0-5 (EventLog.Dispose synchronous) — F2 is a good stage to resolve
- FU-F0-4 (.GetAwaiter().GetResult() at Spectre.Cli boundaries) — convert to `async Task<int>`
- D8: TrackerGenerator.Write cross-platform atomicity — low priority
- D9: RunDb ctor Dispose-during-cleanup exception masking — very low priority
- D10: SeedCheckpoints transaction atomicity — very low priority

### Followups created/updated
- **FU-F1-05** (NEW): `SeedCheckpoints` loop-per-row atomicity. Tracked in `.conductor/followups.md`.
- D7 (RunDb.Query disposed-connection guard) marked CLOSED.

### Gate battery
- Build: 0 warnings, 0 errors
- Tests: 547/547 pass (1 pre-existing flaky FU-F0-6 excluded)
- Format analyzer: 0 issues (warnings-as-errors; no rules suppressed or lowered)

### Evidence paths
- Disposed-connection guard: `src/Conductor/Core/RunDb.cs:517`
- Full schema + write lifecycle: `src/Conductor/Core/RunDb.cs:29-571`
- Tracker-as-view: `src/Conductor/Core/TrackerGenerator.cs:11-160`
- Orchestrator integration: `src/Conductor/Core/Orchestrator.cs:2488-2525`
- CLI verbs: `src/Conductor/Commands/Commands.cs:1646-1813`
- Tests: `tests/Conductor.Tests/RunDbTests.cs` (15 tests, all pass)

---

## Seventh audit (this session, 2026-07-10) — FINAL HARDEN + FINISH

**Gate battery:** 546/548 pass (2 pre-existing flaky FU-F0-6 excluded), build 0w/0e. All gates green.

### Audit scope
Comprehensive fresh-audit of all 14 files in `db9e988..HEAD`. Distinct from prior audits, this session focused on:
- Contract-layer review: each layer's ownership, what crosses boundaries, event/model schemas
- Composition-over-inheritance verification: explicit dependencies over ambient state
- Event-sourced projection integrity: run.db as source of truth, tracker as generated view
- Async/threading audit: `ConfigureAwait(false)` completeness, CT threading, sync-over-async boundaries
- Shallow/stubbed implementation sweep: any `// TODO` in this phase's diff
- Edge-case coverage: empty/null, boundary, concurrency, failure paths across all write paths

### Findings
**No new correctness bugs, race conditions, resource leaks, or async/threading issues found.**
The six prior audit sessions (58f3ba4 through d6dd94d) have thoroughly hardened this phase.
All previous fixes verified clean and intact.

| Area | Verdict |
|---|---|
| RunDb constructor fail-safe (Close+Dispose on schema failure, WAL pragma) | Correct — connection lifecycle is clean; catches all exception paths |
| EnsureSchema transaction rollback completeness | Correct — narrow SqliteException catch + explicit Rollback; non-SqliteException escapes to constructor catch-all which closes the connection (implicit rollback via `using var tx`). All paths safe. |
| CreateSchema version-check early-return correctness | Correct — early return at `ver == CurrentSchemaVersion` is inside the same transaction that created the full schema. DDL is transactional in SQLite. |
| MigrateFrom v1→v2 | Correct — `CREATE TABLE IF NOT EXISTS checkpoints` is idempotent; schema_version updated in same transaction as creation. |
| TryExecute exception contract | Verified against all code paths: catches SqliteException, ObjectDisposedException, InvalidOperationException. No remaining leak paths. Command disposal via `using` prevents resource leaks. |
| Query disposed-connection guard (D7, fixed in 6th audit) | Fixed — `if (_disposed != 0) throw new ObjectDisposedException(nameof(RunDb));` at line 517. Verified present and correct. |
| Dispose idempotency | Two-layer guard: `Interlocked.CompareExchange` + try-catch on `_conn.Close()`/`_conn.Dispose()`. Double-dispose is a safe no-op. |
| RunDb write-after-dispose safety | All 12 DB-write call sites in Orchestrator use `_runDb?.` or `if (_runDb is { } db)` pattern. TryExecute catches disposal exceptions. |
| SeedCheckpoints ON CONFLICT semantics | Correct — updates only title/stage_id on conflict; preserves status/commit/evidence from prior UpdateCheckpoint. Tested at RunDbTests.cs:211. |
| SeedCheckpoints partial-failure (no transaction wrapping) | Acceptable — each UPSERT is idempotent via ON CONFLICT. Restart re-seeds from intact tracker file. No permanent data loss. |
| TrackerGenerator.Generate null-safe plan.Stages | All 3 enumeration sites use `?? []`. Verified. |
| TrackerGenerator.Generate status label | `(status ?? "").ToUpperInvariant()` — null-safe, handles unknown statuses as "TODO". |
| TrackerGenerator.Generate byte-stability | Checkpoint rendering order: plan-defined stages first in plan order, DB-only stages in query order. Deterministic for same inputs. |
| TrackerGenerator.Write cross-platform atomicity | `File.WriteAllText` on NTFS is temp+rename (atomic for tracker-sized files). D8 remains deferred for Linux. |
| EmitSessionFinished handoff persistence ordering | Correct — reads tracker (agent-authored handoff), persists to DB, THEN regenerates tracker from DB. Generated tracker always has latest DB handover. |
| EmitSessionFinished commit SHA extraction | Guarded by `rec.NewCommits.Count > 0` check; falls back to "-". D5/FU-F1-03 tracked. |
| EmitGates gate tier resolution | `plan.Gates.FirstOrDefault(gc => gc.Name == g.Name)?.Tier ?? "full"` — correct default for unknown gates. |
| EmitGates tail truncation safety | `g.Tail.Length > 2000 ? g.Tail[^2000..] : g.Tail` — safe (all GateResult construction sites set non-null string Tail). |
| ReadTrackerSafe exception handling | Widened to `catch (Exception)` in 5th audit fix — verified at all 3 sites (Orchestrator.cs:2509, 1849, 2000). |
| Orc strator RegenerateTracker error handling | Wrapped in try-catch Exception with logged message — tracker regeneration failure never crashes the run. |
| ReportCommand.RunQuery SQL injection | Accepted — diagnostic tool on local DB, operator-audience only. Parameterized queries used internally. |
| ReportCommand.RunQuery Spectre table rendering | Column names from `reader.GetName(i)`, values formatted by .NET type, nulls as `[grey]-[/]`. Markup.Escape prevents injection. |
| TaskCommand/NoteCommand RunDb lifecycle | Each CLI verb opens a new `using` RunDb instance. NoteCommand construction wrapped in try-catch with clean CLI error. |
| NoteCommand.OpenRunDb double-catch | Redundant but harmless — inner transforms to InvalidOperationException, outer formats for CLI. |
| McpServeCommand RunDb wiring + disposal | Best-effort open with catch; `runDb?.Dispose()` in finally. MCP works without run.db. |
| McpTaskServer.HandleNote dual-write (DB + journal) | Correct — DB write is best-effort (empty catch, D6); journal write is durable fallback. Note survives regardless. |
| McpTaskServer.RunAsync stdin loop termination | EOF (null line) or CancellationToken terminates. Error responses for malformed JSON and unknown methods. |
| McpTaskServer.WriteJournal file I/O | Synchronous but called from sync MCP handlers on single-threaded stdin loop. No concurrency issue. |
| Null-safe `_runDb` access across Orchestrator | All 12 call sites verified — `?.` or `is { } db` pattern. No NRE risk. |
| RunDb DI registration in ConductorHost | `return null!` for dry-run, live instance for real runs. Orchestrator resolves via `sp.GetService<RunDb>()` (nullable). |
| Orchestrator.RunAsync exception handling | `OperationCanceledException` caught gracefully; other exceptions from RunSessionAsync are internally handled (all session outcomes return normally). |
| No async void | Zero occurrences across all 14 diff files. |
| No blocking .Result/.Wait() except Spectre.Cli boundary | All 3 sites (`RunCommand`, `McpServeCommand`) use `GetAwaiter().GetResult()` — correct pattern for sync-over-async at entry point. MA0045 pragma with rationale. |
| ConfigureAwait(false) completeness | All async methods in Orchestrator, McpTaskServer, Commands.cs use `.ConfigureAwait(false)` where appropriate. |
| CancellationToken threading | `ct` threaded through Orchestrator loop, `RunSessionAsync`, MCP `RunAsync`, `agent.Kill()` signal handlers. `TryExecute`/`Query` intentionally no-ct (fast local SQLite). |
| Zero TODOs in F1 diff | Confirmed — no `// TODO`, `// FIXME`, `// HACK`, or `// stub` markers in any of the 14 diff files. |
| Zero lowered analyzer severities | All #pragma suppressions have inline rationale (MA0045 for sync I/O at Spectre.Cli or local SQLite; CA1031 for best-effort catch). No severity lowered to pass. |
| Zero comment additions | No spurious comments added in this audit — only this handover update. |

### What remains weak / shortcut / assumed

All 6 prior deferred items (D1–D6) and 4 from follow-on audits (D8–D10, FU-F1-01–FU-F1-05) remain valid and unchanged. D7 was closed in the 6th audit.

No new deferred items identified this session.

### Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit |
|---|---|---|---|
| None found | — | N/A | — |
| HostLoggingTests.* flaky (2 tests, FU-F0-6) | LOW | NO — pre-existing | N/A |

### Risks for F2 (cumulative, unchanged)

- Verifier scoring loop (F4) needs `scores` table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: test under concurrent load with WAL single-writer model
- `conductor_note` MCP tool's TaskAdded journal pollution (D2, FU-F1-02)
- TrackerGenerator dependency graph renders only explicit `DependsOn` edges
- FU-F0-5 (EventLog.Dispose synchronous) — F2 is a good stage to resolve
- FU-F0-4 (.GetAwaiter().GetResult() at Spectre.Cli boundaries) — convert to `async Task<int>`
- D8: TrackerGenerator.Write cross-platform atomicity — low priority
- D9: RunDb ctor Dispose-during-cleanup exception masking — very low priority
- D10: SeedCheckpoints transaction atomicity — very low priority

### Followups
No new followups created. All prior followups (FU-F1-01 through FU-F1-05, FU-F0-1 through FU-F0-6) remain tracked in `.conductor/followups.md`.

### Evidence paths
- Contract-level review: `src/Conductor/Core/RunDb.cs` (class doc + 571 lines), `src/Conductor/Core/TrackerGenerator.cs` (class doc + 160 lines)
- Layer ownership: run.db owns schema + writes + queries (data layer); TrackerGenerator owns view rendering (projection layer); Orchestrator owns integration (orchestration layer); McpTaskServer + CLI verbs own external contract (API layer)
- Trust model: event-sourced spine (events.jsonl) is the durable record; run.db is the queryable projection; tracker.md is the generated view. Writes are additive-best-effort to run.db — events.jsonl is the fallback truth source
- Schema: `src/Conductor/Core/RunDb.cs:71-284` (12 tables, v2)
- Tests: `tests/Conductor.Tests/RunDbTests.cs` (15 tests, all pass), `tests/Conductor.Tests/McpTaskServerTests.cs` (10 tests, all pass)

---

## Eighth audit (this session, 2026-07-10) — FINAL ARCHITECTURE HARDEN

**Gate battery:** 548/548 pass, build 0w/0e. All gates green.

### Audit scope
Full independent static audit of all 14 files in `db9e988..HEAD`. Focus areas:
- Exit-path completeness: every run.db integration point covered on ALL exit/failure paths
- State synchronization: run.db status vs state.json status vs events.jsonl
- Layer boundaries: data layer (RunDb), projection layer (TrackerGenerator), orchestration layer (Orchestrator), API layer (McpTaskServer + CLI verbs)
- Seven prior audit fixes re-verified intact

### What this audit FIXED

| # | Issue | Severity | File | Fix |
|---|-------|----------|------|-----|
| 1 | **Missing `RecordRunEnd` on Aborted exit path** (`Orchestrator.cs:99`): The `RunAsync` main loop returned from the Aborted path with `SaveAndReport()` but never called `_runDb?.RecordRunEnd`. This left the `runs` table showing `status='running'` after the run was genuinely aborted. All other exit paths (CompletePlan, --once, --max-sessions, cancellation) were either already covered or semantically correct (non-terminal states). | LOW | Orchestrator.cs:99 | Added `_runDb?.RecordRunEnd(state.RunId, state.Status.ToString())` before `SaveAndReport()` |

### Findings verified clean (fresh full audit)

| Area | Verdict |
|---|---|
| `CompletePlan` → `RecordRunEnd` | Correct — writes `status='Completed'` + `ended_utc` |
| `NeedsHuman` → no `RecordRunEnd` | Correct — NeedsHuman is NOT a terminal state; the run can be resumed. `ended_utc` should NOT be set. Status gap is deferred (see D11) |
| `Aborted` → `RecordRunEnd` | **NOW FIXED** — writes `status='Aborted'` + `ended_utc` (run is genuinely terminated) |
| `StopAfterSession` → return 0 | Correct — status is `Paused`, not terminal; no `RecordRunEnd` needed |
| `--once` → return 0 | Correct — status is `Running`, no `RecordRunEnd` needed |
| `--max-sessions` → return 0 | Correct — status is `Running`, no `RecordRunEnd` needed |
| Cancellation → return 130 | Correct — run is interrupted but resumable; `SaveAndReport()` captures state; no `RecordRunEnd` needed |
| Exception in `RunAsync` outer try | Pre-existing — uncaught exception terminates the process; `finally { ReleaseLock() }` runs; no run.db write possible (process is crashing) |
| `SeedCheckpointsFromTracker` silent-fail safety | Verified — if `ReadTrackerSafe()` returns empty (transient error), `SeedCheckpointsFromTracker` returns early with no-op. The main loop's authoritative `_progress.Read(plan, ct)` at line 150 would also fail and raise `NeedsHuman`, so no session runs with missing checkpoints. The race window between line 150 and line 2494 is effectively zero |
| `RegenerateTracker` handoff round-trip | Verified — agent writes handoff to tracker file → `EmitSessionFinished` reads tracker → persists handoff to DB via `WriteHandover` → regenerates tracker FROM DB. The generated tracker always has the latest DB handover (or the agent-written text as fallback) |
| `EmitSessionFinished` checkpoint update ordering | Verified — `UpdateCheckpoint` (for each NewlyDone checkpoint) runs BEFORE `RegenerateTracker`. Generated tracker correctly reflects DONE status. No data loss in the round-trip |
| `EmitGates` gate tier resolution + tail truncation + SHA | All verified clean |
| `EmitSessionFinished` commit SHA `rec.NewCommits[^1].Split(' ')[0]` | Guarded by `rec.NewCommits.Count > 0`; falls back to "-" when empty. Verified at line 1902 |
| `RunDb.TryExecute` exception contract | Verified — catches SqliteException, ObjectDisposedException, InvalidOperationException; all write methods are additive-best-effort |
| `RunDb.Query` disposed-connection guard | Verified — `if (_disposed != 0) throw new ObjectDisposedException(nameof(RunDb))` at line 517 (D7 fix from 6th audit) |
| `RunDb.Dispose` idempotency | Verified — `Interlocked.CompareExchange` guard, try-catch on Close+Dispose |
| `TrackerGenerator.Generate` null-safe | All 3 plan.Stages enumerations use `?? []` |
| `TrackerGenerator.StatusLabel` null-safe | `(status ?? "").ToUpperInvariant()` handles null |
| `TrackerGenerator.Write` | `File.WriteAllText` with UTF8 BOM; NTFS temp+rename atomicity |
| `RunDb` DI registration (ConductorHost) | `return null!` for dry-run; live path resolves correctly; Orchestrator uses `sp.GetService<RunDb>()` (nullable) |
| `McpServeCommand` RunDb wiring + disposal | Best-effort open; `runDb?.Dispose()` in finally |
| `McpTaskServer.HandleNote` dual-write | DB best-effort (empty catch, D6); journal fallback |
| `ReportCommand.RunQuery` | Parameterised queries internally; raw SQL from CLI is diagnostic-only |
| `TaskCommand` / `NoteCommand` RunDb lifecycle | Each opens new `using` instance; construction errors caught |
| All 7 prior audit fixes | Re-verified intact — no regressions |
| Zero TODOs in F1 diff | Confirmed — no `// TODO`, `// FIXME`, `// HACK`, or `// stub` markers |
| Zero lowered analyzer severities | All #pragma suppressions have inline rationale |
| Async flow | No `async void`, all `ConfigureAwait(false)`, no blocking `.Result`/`.Wait()` except at Spectre.Cli boundary |
| CancellationToken threading | All cancellable paths threaded correctly |

### What remains genuinely weak / shortcut / assumed

All prior deferred items (D1–D10, FU-F1-01 through FU-F1-05) remain valid and unchanged. D7 was closed in the 6th audit.

**New deferred items from this audit:**

| # | Item | Severity | Followup |
|---|---|---|---|
| D11 | **run.db `runs.status` not updated on non-completion non-terminal states** (`NeedsHuman`, `Paused`, `AwaitingOwner`, `VerifyingGates`, `Backoff`). `RecordRunEnd` correctly sets `ended_utc` which these states don't warrant (the run can be resumed). A future `UpdateRunStatus` method (updating status without `ended_utc`) would keep run.db queries accurate between resume cycles. Low severity: `state.json` is the authoritative status source; run.db is additive/best-effort; on resume, `InitializeRun` (INSERT OR REPLACE) correctly re-initializes the row. | LOW | FU-F1-06 |

### Bugs found and resolution

| Bug | Severity | Fixed? | Fix commit |
|---|---|---|---|
| Missing RecordRunEnd on Aborted exit path | LOW | YES | This session |
| HostLoggingTests flaky (2 tests, FU-F0-6) | LOW | NO — pre-existing | N/A |

### Risks for F2 (cumulative + updated)

- Verifier scoring loop (F4) needs `scores` table — schema ready, completely unused
- Lane pool workers (F2.1) writing run.db: test under concurrent load with WAL single-writer model
- `conductor_note` MCP tool's TaskAdded journal pollution (D2, FU-F1-02)
- TrackerGenerator dependency graph renders only explicit `DependsOn` edges
- FU-F0-5 (EventLog.Dispose synchronous) — F2 is a good stage to resolve
- FU-F0-4 (.GetAwaiter().GetResult() at Spectre.Cli boundaries) — convert to `async Task<int>`
- D8: TrackerGenerator.Write cross-platform atomicity — low priority
- D9: RunDb ctor Dispose-during-cleanup exception masking — very low priority
- D10: SeedCheckpoints transaction atomicity — very low priority
- **D11 (NEW):** run.db status not updated on non-terminal states (NeedsHuman, Paused, etc.) — low priority

### Followups created/updated
- **FU-F1-06** (NEW): run.db runs.status not updated on non-completion non-terminal states. Tracked in `.conductor/followups.md`.
- D7 (RunDb.Query disposed-connection guard) — previously CLOSED.

### Evidence paths
- Aborted exit-path fix: `src/Conductor/Core/Orchestrator.cs:99`
- Full exit-path analysis: `src/Conductor/Core/Orchestrator.cs:65-366` (RunAsync)
- RunDb schema + writes: `src/Conductor/Core/RunDb.cs:29-571`
- Tracker-as-view: `src/Conductor/Core/TrackerGenerator.cs:11-160`
- Tests: `tests/Conductor.Tests/RunDbTests.cs` (15 tests), McpTaskServerTests (10 tests) — all pass

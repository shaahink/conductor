# F0 — Foundations: audit handover

**Date:** 2026-07-10 | **Session:** Audit #5 (re-audit hardening) | **Branch:** `feat/foreman`

## What is solid and proven

### Kill list (F0.1) — COMPLETE, proven
- **Replay/Confidence classes deleted** — `Confidence.cs`, `Replay.cs`, `ReplayTests.cs` removed (687 net LOC deleted across 23 files). Zero remaining references in C# source code. Tests updated (B5_4Tests, B11_2Tests).
- **Personas trimmed 9→3** — `PersonaRegistry.BuiltIns` now has exactly `deliver`, `verify`, `advise`. All tests referencing old names updated.
- **Heartbeat git commits removed** — `ToggleHeartbeat` control action, `HeartbeatCommand` CLI verb, dashboard `H` key, `HeartbeatOn` snapshot field, `_heartbeatToggleValue`/`_originalHeartbeatMinutes` fields all deleted. Report still refreshes to disk mid-session via `RefreshReport()`; no commits touch the feature branch.
- **Template system collapsed** — `TemplatesDir` property removed from `PlanConfig`. `NewPlanCommand` now produces a single minimal scaffold. `NewPlanScaffoldTests` deleted. Prompt builder resolves templates from plan dir directly.
- **Gate:** 533 tests pass, 0w/0e.

### Async control loop (F0.2) — COMPLETE, proven (hardened in sessions #4 and #5)
- `Orchestrator.Run()` → `RunAsync(CancellationToken ct)` — returns `Task<int>`.
- 9 private methods converted to async, 6 `Thread.Sleep` → `Task.Delay`, 3 `.GetAwaiter().GetResult()`/.Result → `await`.
- `Commands.cs` callers updated (sync `Execute` boundaries use `.GetAwaiter().GetResult()` with `#pragma`). Test callers (`HostLoggingTests`) updated.
- **Hardened session #4:** 7 `CancellationToken.None` delay sites → `ct` with OCE handling (commits `1af1ffb`, `b5b3baf`).
- **Hardened session #5 (this session):** `ApproveAwaitingOwnerAsync` now accepts and threads `ct` (was passing `CancellationToken.None` to `ConfirmStageAsync`); post-hook in `ConfirmStageAsync` now passes `ct`; removed redundant `Task.Run()` wrapping in `RunFollowupFixLanesAsync` (pre-F0 leftover).
- **Gate:** 533 tests pass, 0w/0e.

### Integration harness (F0.3) — COMPLETE, proven
- `HarnessTests.cs` — 2 tests: full cycle (fake cmd agent writes opencode JSON, creates deliverable, git commits) + dry-run (no modifications).
- Requires `git` on PATH (creates temp repos).
- **Gate:** 533 tests pass, 0w/0e.

## What was FIXED in this re-audit (session #5)

| Issue | Severity | Fix |
|-------|----------|-----|
| `ApproveAwaitingOwnerAsync` → `ConfirmStageAsync(stageId, CancellationToken.None)` — owner approval path could not be cancelled | MEDIUM | Added `CancellationToken ct` parameter to `ApproveAwaitingOwnerAsync`; threaded from `HandleControlAsync(ct)` → `ApproveAwaitingOwnerAsync(ct)` → `ConfirmStageAsync(stageId, ct)` |
| `RunStageHook(id, "post-hook", postHook, CancellationToken.None)` in `ConfirmStageAsync` — post-hook ignored the method's CT | LOW | Changed to `RunStageHook(id, "post-hook", postHook, ct)` — hook now respects cancellation |
| `Task.Run(() => MutatingLaneRunner.RunAsync(...), ct)` in `RunFollowupFixLanesAsync` — redundant wrapping of an already-async method | LOW (cleanup) | Removed `Task.Run` wrapping; `MutatingLaneRunner.RunAsync` is `async Task` — unnecessary thread-pool offload |

**Commit:** `be10727` — all fixes in this session.

## What remains shortcut / weak / assumed

### CancellationToken gaps (pre-existing, recorded for followup)
- **FU-F0-1**: `RunStageHook` post-hook `CancellationToken.None` — **CLOSED** this session (`be10727`).
- **FU-F0-2**: `Task.Run` in `StartParallelAudit` passes CT but audit can't be cancelled once launched — no `_parallelAuditCts` to cancel mid-flight. Pre-existing.
- **FU-F0-3**: `TelegramService` fire-and-forget patterns (`_ = PushAsync/PushWithKeyboardAsync`) have no fault continuation. Pre-existing; internal methods catch exceptions.
- **FU-F0-x**: `SaveAndReport()` and `PushIdleSnapshot()` use `_progress.Read(plan, CancellationToken.None)` because they are sync methods called from the async flow — CT is not available at the call site. Low risk (display-only reads with tolerant fallback).

### Async boundary debt (pre-existing from before F0)
- **FU-F0-4**: 3× `.GetAwaiter().GetResult()` at Spectre.Cli sync boundaries (`Commands.cs:80, :91, :1424`). Currently safe (no `SynchronizationContext` in console apps) but fragile. Spectre.Cli supports `async Task<int> ExecuteAsync` — convert when the next phase touches Commands.cs.
- **FU-F0-5**: `EventLog.Dispose()` calls `_drain.GetAwaiter().GetResult()` — blocks on shutdown. Pre-existing; not in F0 diff scope.

### Test flakiness
- **FU-F0-6**: `HostLoggingTests.DryRunWritesJsonLogWithCorrelationProperties` is flaky (file-share race between parallel test runs). Passes on retry. Pre-existing.

### Docs not updated for persona rename
- Stage B7 docs still reference old persona names. These are design docs consumed by agents, not the engine. The agent prompt already includes the available personas.

## Bugs found and status

| Bug | Fixed? | Detail |
|-----|--------|--------|
| CancellationToken.None in idle delays (7 sites) | YES — session #4 | Responsiveness regression from F0.2 async conversion |
| CT not threaded to fix-lanes/lane-artifacts | YES — session #4 | Could block indefinitely during Ctrl+C |
| CT not threaded to tracker reads in async flow | YES — session #4 | Low risk but correct pattern |
| ApproveAwaitingOwner→ConfirmStage used CancellationToken.None | YES — this session (#5) | Owner-approval path uncancellable |
| Post-hook used CancellationToken.None in ConfirmStageAsync | YES — this session (#5) | Post-hook ignored parent CT |
| Redundant Task.Run in RunFollowupFixLanesAsync | YES — this session (#5) | Pre-F0 leftover wrapping |
| Flaky HostLogging test | DEFERRED — FU-F0-6 | Pre-existing file-share race |

## Risks for F1 (run.db)

1. **SQLite introduces file-locking**: The `run.db` write path must coexist with `events.jsonl` and `state.json` writers. Verify no file-lock contention under concurrent drain.
2. **Tracker-as-view must be byte-stable**: Generated `TRACKER.md` must be deterministic (no timestamps, no GUIDs) or the normalizer will produce noise commits. F0's killed heartbeat commit machinery was the old source of noise — the new generator must not re-introduce it.
3. **CT threading**: Continue the pattern established here — every new async method should accept and respect `CancellationToken`. The remaining `CancellationToken.None` sites in `SaveAndReport`/`PushIdleSnapshot` (sync methods without CT access) should be resolved when those methods are made async.

SESSION-RESULT: Audit verdict PASS with fixes. Fixed 3 additional CT-gap sites: threaded CT through ApproveAwaitingOwner→ConfirmStage chain, post-hook now respects parent CT, removed redundant Task.Run wrapping in fix-lanes. All 533 tests green, 0w/0e. FU-F0-1 closed. 5 followups remain (all pre-existing: uncancellable parallel audit, Telegram fire-and-forget, Spectre.Cli sync boundaries, EventLog.Dispose blocking, HostLogging test flakiness). F0 phase is production-ready for F1 handoff.
